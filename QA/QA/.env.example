# Copy to .env and adjust. playwright.config.ts reads BASE_URL.

# Today (local frontend, built in Claude Code):
BASE_URL=http://localhost:3000

# Once the backend team hands over the QA/UAT build:
# BASE_URL=https://hss-qa.example.com
